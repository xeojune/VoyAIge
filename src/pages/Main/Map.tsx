import React from "react";
import { MapContainer } from "../../styles/Map";

const Map: React.FC = () =>{
    return(
        <MapContainer>
            Map
        </MapContainer>
    );
};

export default Map;